<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.3.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">

    <!--
        开启SpringMVC注解功能，即@Controller
        com.zhenghou.teaching.controller.IndexController
    -->
    <mvc:annotation-driven />
    <!--
        配置控制层扫描 ，spring mvc 启动时，扫描所有指定包以及子包下面包含：@Controller注释的类，建立URL与对应方法的映射关系，
        方便前端调用的时候，通过URL就可以定位到对应的执行方法
        如：/json 对应的方法为 com.zhenghou.teaching.controller.IndexController.json
    -->
    <context:component-scan base-package="com.lazy.ssm">
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>
    <!-- 对静态资源文件的访问， 将无法mapping到Controller的path交给default servlet handler处理 也就是默认服务器 -->
    <mvc:default-servlet-handler />

    <!--
        解析视图层,这里主要是解析jsp,当然也可以整合Thymeleaf框架，相对jsp来说，Thymeleaf效率更高效，
        当Controller返回的时候，不需要写jsp后缀，spring mvc会自动找到对应的JSP文件
    -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"></property>
        <property name="suffix" value=".jsp"></property>
    </bean>

<!--     各种类型转换器的配置 -->
    <bean class="org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerAdapter">
        <property name="messageConverters">
            <list>
                <bean class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter"/>
                <bean class="org.springframework.http.converter.ByteArrayHttpMessageConverter"/>
                <bean class="org.springframework.http.converter.xml.SourceHttpMessageConverter"/>
                <bean class="org.springframework.http.converter.FormHttpMessageConverter"/>
                <bean class="org.springframework.http.converter.StringHttpMessageConverter"/>
            </list>
        </property>
    </bean>

    <mvc:annotation-driven >
        <!-- 消息转换器，添加对文字编码的全局处理，解决中文返回问号的毛病 -->
        <mvc:message-converters register-defaults="true">
            <bean class="org.springframework.http.converter.StringHttpMessageConverter">
                <property name="supportedMediaTypes" value="text/html;charset=UTF-8"/>
            </bean>
        </mvc:message-converters>
    </mvc:annotation-driven>
    <mvc:default-servlet-handler/>
</beans>